# Examples

## TODOs

1. Update `go.mod`
    - If applicable with `<repo-name>` of the project repository.
    - Update module name from `example.com/examples` to `<repo-url>/examples`
2. Update `main.go` package reference from `example.com/examples/internal` to `<repo-url>/examples/internal`
3. Flesh out examples in `examples` directory.
